/**
 * Route group: Maps.
 * Isolated so `mapbox-gl` — the single largest dependency — is only ever
 * downloaded by users who actually open a map.
 */
export { default as FindCleaner } from "@/pages/FindCleaner";
